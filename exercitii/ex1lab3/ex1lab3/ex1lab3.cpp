/* A program : 
----that replaces any sequence of whitespace that does not contain newline with a
single space character
----whitespace characters immediately before a newline are deleted.*/

#include <stdio.h>
#include <string.h>

int ReplaceNL() {

	int c = getchar();

	while ((c = getchar()) != EOF) {

		if ( (strchr(c, '/n') && (strchr(c, '  ') == 0) {

	              
		  }
	 }
}


int main(void) {


	return 0;
}